// This is standard go package with function main.
// It is provided in order to emulate real project package structure.
// In order to run tests use command: go test lwweg/graph
package main

import (
	"fmt"
	_ "lwweg/graph"
)

func main() {
	fmt.Println("LWW-Element-Graph application")
}
